<nav class="bg-white shadow p-4 mb-6">
  <div class="max-w-2xl mx-auto flex space-x-4">
    <!-- Home -->
    <a href="/" class="hover:underline">
      Home
    </a>

    <!-- Transaktionen -->
    <a href="/transactions" class="hover:underline">
      Transaktionen
    </a>

    <!-- Admin: je nach eingeloggtem Status zur Übersicht oder zum Login -->
    <% if (admin) { %>
      <a href="/admin/transactions" class="hover:underline">
        Admin
      </a>
    <% } else { %>
      <a href="/admin/login" class="hover:underline">
        Admin
      </a>
    <% } %>

    <!-- Logout nur anzeigen, wenn jemand eingeloggt ist -->
    <% if (userVorname || admin) { %>
      <a href="/logout" class="ml-auto text-red-600 hover:text-red-800">
        Logout
      </a>
    <% } %>
  </div>
</nav>